import Link from 'next/link';
import { SITE_NAME } from '../../config/app-config';
import { Container } from './styled';

const Footer = () => {
  return (
    <Container>
      <span>Feito por Pedro Ferreira</span>
    </Container>
  );
};

export default Footer;
